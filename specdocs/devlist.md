1. for perlin noise add W offset


6. Add notion of UVs for image generators
7. Add Math node with all operations (allow float and image data)
8. ADd displace node
9. For merge node add all other blend modes
10. RGB seperate node



12. Add right click menu for nodes and a "Code Editor" button. hitting that opens that code of that node in a IDE code editor in the parameters window

13. Array node, Create grid based, linear, radial, spiral, concentric, arrays. of given input objects. 

14 merge node needs to respect alpha of incoming datastream, and so that prob means we need to make sure an alpha stream carries through node chain from any generator type node