1. for perlin noise add W offset
2. transform node
3. copy image shortcut. 
4. output node has export settings shown in panel. and export buttons on node for image. video
5. in node graph, two finger scroll should pan. Click and drag should do box/marque select
6. Add notion of UVs for image generators
7. Add Math node with all operations (allow float and image data)
8. ADd displace node
9. For merge node add all other blend modes
10. RGB seperate node
11.